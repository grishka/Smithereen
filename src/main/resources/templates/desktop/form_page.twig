{%extends "page.twig"%}
{%block content%}
<center class="notMuchContent">
<form method="post" action="{{action}}">
	<div>{%include concat(contentTemplate, ".twig")%}</div>
	<input type="hidden" name="csrf" value="{{csrf}}"/>
	<input type="submit" value="{{submitButton}}"/>
</form>
</center>
{%endblock%}