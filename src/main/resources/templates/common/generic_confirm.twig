{%extends "page"%}
{%block content%}
<center class="notMuchContent">
	<form action="{{formAction}}" method="post">
		<p>{{message}}</p>
		<input type="hidden" name="csrf" value="{{csrf}}"/>
		<input type="submit" value="{{L('yes')}}"/>
		<a href="{{back}}" class="button secondary" data-no-al>{{L('no')}}</a>
	</form>
</center>
{%endblock%}