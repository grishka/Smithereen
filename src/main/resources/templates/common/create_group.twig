{% import "forms" as form %}
{{ form.start('createGroup', createGroupMessage) }}
	{{ form.textInput('name', L('group_name'), groupName, {'maxlength': '200', 'required': true, 'autocomplete': false}) }}
	{{ form.textArea('description', L('group_description'), groupDescription) }}
{{ form.end() }}