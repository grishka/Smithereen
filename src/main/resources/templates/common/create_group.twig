{% import "forms" as form %}
{{ form.start('createGroup', createGroupMessage) }}
	{{ form.textInput('name', L('group_name'), groupName, {'maxlength': '200', 'required': true, 'autocomplete': false}) }}
	{{ form.textInput('username', L('group_username'), groupUsername, {'maxlength': 50, 'required': true, 'pattern': '^[a-zA-Z][a-zA-Z0-9._-]+$', 'autocomplete': false, 'explanation': L('username_explain'), 'prefix': "#{serverDomain}/"}) }}
{{ form.end() }}