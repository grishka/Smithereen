{# @pebvariable name="user" type="smithereen.model.User" #}
{% extends "page" %}
{% block content %}
<div class="notMuchContent">
	<h2 class="marginAfter">{{ L('profile_hidden') }}</h2>
	<p>{{ L('profile_hidden_info') }}</p>
	<p>
		<a href="/account/login">{{ L('log_in') }}</a>
		{% if user.domain is not empty %}| <a href="{{ user.url }}" target="_blank" rel="nofollow">{{ L('open_on_server_X', {'domain': user.humanReadableDomain}) }}</a>{% endif %}
	</p>
</div>
{% endblock %}